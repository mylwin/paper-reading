# 论文工作区文件架构规范

本项目用于保存论文原文、PDF 解析结果、精读笔记和公式解读。所有论文资产均为真实文件、不使用符号链接；新建论文资料必须遵循本规范。

## 命名原则

- `<论文标题>` 是不带扩展名、可安全用作文件和文件夹名的稳定标识。
- 对同一篇论文，`01-raw` 与 `02-markdown` 使用相同的 `<论文标题>` 文件名主干，仅扩展名不同。
- `03-notes` 的论文专属文件夹使用同一个 `<论文标题>`；其内部笔记文件通常不再重复添加论文标题前缀。
- 保持现有历史命名，不为统一样式擅自重命名或移动；如需改名或移动必须先征得明确要求，并同步校验所有链接。

## 文件归属与符号链接

所有资产都是所属目录下的**真实文件**，一律禁止使用符号链接（symlink）。

- 不要用符号链接作为跨目录引用的"捷径"或旧名别名，例如 `03-notes/<论文标题>/xxx.md -> ../../...`、`01-raw/旧名.pdf -> 新名.pdf`。
- 不要用自引用符号链接伪造目录结构，例如 `images/<论文标题> -> .`。
- 跨目录引用（其他论文的笔记、图片、原文 PDF/Markdown）一律使用**相对路径**直接指向真实文件本身，不经过符号链接中转。
- 图片真实存放于 `03-notes/<论文标题>/images/`（引用文献图片放 `images/<被引用论文标题>/`），不使用链接替代。
- 移动或删除真实文件时，同步更新所有引用它的相对链接，确保不存在悬空引用。

## 01-raw：原始论文

`01-raw/` 仅保存用户提供或下载的原始 PDF。

```text
01-raw/
└── <论文标题>.pdf
```

- 不在此目录保存解析文本、笔记或图片副本。
- 新文件应保留 PDF 原始内容，不修改或覆盖用户已有 PDF。

## 02-markdown：PDF 解析结果

`02-markdown/` 仅保存从 PDF 解析得到的 Markdown。

```text
02-markdown/
└── <论文标题>.md
```

- 文件名主干必须与对应 `01-raw/<论文标题>.pdf` 一致。
- 解析 Markdown 是论文内容来源，不是人工精读笔记；不要在其中混入个人批注或 AI 分析。

## 03-notes：论文阅读笔记

每篇论文必须拥有自己的目录。AI 生成的精读固定命名为 `精读.md`；用户补充的阅读想法、问题、复现实验等也放在同一论文目录，使用内容语义明确的文件名。

```text
03-notes/
└── <论文标题>/
    ├── 精读.md
    ├── <用户笔记主题>.md
    └── images/
        ├── fig1.png
        ├── fig2.png
        └── <被引用论文标题>/
            ├── fig1.png
            └── fig2.png
```

### 图片管理决策

采用**每篇论文目录内管理图片**的方式，不使用全局的 `03-notes/images/` 图片池。

- 本篇论文的图片直接放在 `03-notes/<论文标题>/images/` 下，例如 `fig1.png`、`fig2-method.png`。
- 引用其他论文的图片，放在 `images/<被引用论文标题>/` 下，防止与本篇图片或其他引用文献的图片同名冲突。
- 不要为没有图片的本篇或引用文献创建空图片目录。
- 笔记使用标准相对路径：

```markdown
![本篇论文图](images/fig1.png)
![引用文献图](images/<被引用论文标题>/fig1.png)
```

- 图片必须保存为本地文件并使用标准 Markdown 图片语法；不要依赖远程图片 URL、绝对路径、特定笔记软件语法或符号链接。
- 将图片与论文笔记置于同一目录树，便于整篇笔记连同资产一起移动、备份和分享。

## 04-equation_problem：公式解读

目录名 `04-equation_problem/` 用于存放论文的公式解读。每篇论文在此目录下建立对应文件夹；该文件夹是这篇论文全部公式相关材料的唯一归属位置。

```text
04-equation_problem/
└── <论文标题>/
    ├── <AI 公式伴读或全流程解读>
    ├── <用户的理解与推导>
    └── <扫描图片、附件或其他公式材料>
```

- 不按单条公式强制拆分文件，也不限制该论文文件夹内的文件名、文件格式或子目录结构。
- 该目录可以包含 AI 的公式全流程解读、AI 伴读解释、用户自己的理解与推导、扫描得到的图片或文档，以及其他与公式有关的材料。
- 只要求所有内容都位于对应的 `<论文标题>/` 下；不要把同一篇论文的公式材料散落在 `04-equation_problem/` 根目录或其他论文目录。
- 公式解读可链接到对应的 `02-markdown/<论文标题>.md` 或 `03-notes/<论文标题>/精读.md`，无需复制整篇论文内容。

## 执行检查清单

创建或更新论文资料前，依次确认：

1. `01-raw` 中存在对应 PDF，且 `02-markdown` 使用相同文件名主干。
2. `03-notes/<论文标题>/精读.md` 是 AI 精读的唯一固定入口。
3. 本篇及引用文献的图片均位于该论文目录的 `images/` 树中。
4. 所有该论文的公式推导、AI 伴读、用户材料和相关附件都位于 `04-equation_problem/<论文标题>/` 下。
5. 所有 Markdown 图片链接与跨目录引用均为可解析的相对路径，且指向真实文件；不覆盖用户已有笔记。
6. 工作区内不存在任何符号链接。

## 单篇论文的四目录对照示例

以论文 `<论文标题>` 为例，四份目录的对应关系：

```text
01-raw/
└── <论文标题>.pdf

02-markdown/
└── <论文标题>.md

03-notes/
└── <论文标题>/
    ├── 精读.md
    ├── <用户笔记主题>.md
    └── images/
        ├── fig1.png
        └── <被引用论文标题>/
            └── fig1.png

04-equation_problem/
└── <论文标题>/
    └── <公式解读、推导、附件等>
```

用户在当前对话中给出的明确要求优先于本文件；发生冲突时，应先向用户确认再修改布局。
