# 论文工作区文件架构规范

本文件是论文工作区的**唯一总规范**。项目内所有 `README.md`、skill 文档、脚本路径与落盘行为都必须服从本规范；若某处描述与本文件冲突，以本文件为准，并应同步修正该处。

本项目用于保存论文原文、PDF 解析结果、精读笔记、公式解读和翻译结果，另设书籍、调研、日报与精读过程目录。所有论文资产均为真实文件、不使用符号链接；新建论文资料必须遵循本规范。

## 项目结构整洁规范

- 项目根目录不得创建 `findings.md`、`progress.md`、`task_plan.md` 等临时规划或过程记录文件。
- 需要规划任务时，规划内容应放在外部临时目录，任务完成后不得残留在项目结构中。
- 项目根目录只保留以下文件与目录：`.AGENT.md`、`README.md`、`01-raw/` ~ `08-reading/` 各资料目录，以及 `.claude/`、`.agents/`、`.dsh/` 等技能与工具配置目录。

## 命名原则

- `<论文标题>` 是论文的稳定文件主干（paper_stem）：不带扩展名、不带日期、可安全用作文件和文件夹名。
- **同一篇论文在全部资料目录中使用同一个 `<论文标题>`**：`01-raw` 与 `02-markdown` 用它加扩展名，`03-notes`、`04-equation_problem`、`06-translation`、`08-reading` 用它作为论文专属文件夹名。
- 论文文件名中**不出现日期**；入库日期、精读日期、翻译日期等具体日期一律记录在各级 `README.md` 与根 `index.md` 的表格中。
- `03-notes` 论文目录内的笔记文件通常不再重复添加论文标题前缀（AI 精读固定为 `精读.md`）。
- `06-translation` 论文目录内的翻译文件按翻译形式命名，例如 `双栏对比`、`单独翻译`。
- 结构性调整（按月份分层、统一主干）属于本规范要求，实施时必须**同步更新所有引用**；单篇论文的改名或移动仍需用户明确要求后再执行。
- 仅有两处例外允许文件名/目录名带日期：`07-research/YYYY-MM-DD-<调研主题>.<扩展名>` 与 `08-daily/YYYY-MM-DD/`。

## 月份目录与月份归属

`01-raw`、`02-markdown`、`03-notes`、`06-translation` 四个论文资料目录按 `YYYY-MM/` 月份分目录组织：

```text
<目录>/
├── README.md
├── index.md
└── YYYY-MM/
    ├── README.md
    └── <论文标题>.<扩展名>（或 <论文标题>/ 论文目录）
```

- **月份依据论文首次进入 `01-raw` 的日期**确定，即 `<目录>/<YYYY-MM>/` 中的 `YYYY-MM` 等于该论文入库日期的年月。
- 同一篇论文在四个阶段必须使用**相同的月份**与**相同的主干**，不得出现「01-raw 在 2026-07、解析结果在 2026-09」这类错位。
- 月份目录名严格使用 `YYYY-MM`（两位月份，如 `2026-07`），不使用 `2026-7` 或其他形式。
- `04-equation_problem` 与 `08-reading` **不按月份组织**，继续只按 `<论文标题>/` 建立论文目录。
- 每个月度阶段目录都必须有 `README.md`，用于维护该月的论文清单与待处理状态。
- 论文尚未产生的月份目录只保留 `README.md`，**不要创建空的论文资料目录**（例如未翻译时只登记状态，不预先建 `06-translation/YYYY-MM/<论文标题>/`）。

## 目录说明文件与索引

- 每个编号根目录（`01-raw` ~ `08-reading`）与项目根目录都必须有 `README.md`，说明本目录用途、目录规则、命名规范与相对链接写法。
- 每个 `YYYY-MM/` 月份目录都必须有 `README.md`，说明本月范围、论文清单、状态与待处理事项。
- `01-raw`、`02-markdown`、`03-notes`、`06-translation` 各自维护根级 `index.md` 作为该阶段的总索引；`01-raw/index.md` 是跨阶段统一论文登记表。
- 索引与月份 README 中的机器可读表格统一用标记包裹，标记内由脚本生成，标记外可自由手写：

```markdown
<!-- INDEX:BEGIN -->
| 月份 | 入库日期 | 论文标题 | PDF | 解析 | 精读 | 翻译 |
|---|---|---|---|---|---|---|
| 2026-09 | 2026-09-17 | Powell-Style … | [PDF](2026-09/….pdf) | 已解析 | 已精读 | 已翻译 |
<!-- INDEX:END -->
```

- 状态字段使用固定词表，不得自造同义词：
  - 解析状态：`未解析` / `已解析` / `解析失败`
  - 精读状态：`未精读` / `精读中` / `已精读`
  - 翻译状态：`未翻译` / `翻译中` / `已翻译`
- 未解析、未精读、未翻译的论文**必须出现在对应阶段的索引与月份 README 中**；解析失败必须写明失败原因，精读中/翻译中应写明开始日期。
- 索引中的状态与路径由磁盘实际文件推导；精读开始/完成日期优先取 `精读.md` frontmatter 的 `date` 字段，其次才是 git 提交日期与文件时间。脚本推不出的单元格（`优先级`、`精读中`、人工填写的日期与失败原因）在刷新时按同一主干保留，不会被清空。
- 维护命令：

```bash
python .claude/skills/paper-daily/scripts/sync_indexes.py              # 刷新四份索引与月份 README 表格
python .claude/skills/paper-daily/scripts/sync_indexes.py --check-links # 校验所有相对链接与状态一致性
```

## 文件归属与符号链接

论文资料资产都是所属目录下的**真实文件**，一律禁止使用符号链接（symlink）。

技能发现入口是唯一例外：`.claude/skills/` 保存唯一实体，其他 agent 工具目录下的 `skills` 可以通过符号链接指向它，
以同时兼容 Codex、dsh、Claude Code、pencode、qoder、trae 等工具的项目级 skill 扫描规则：

```text
.agents/skills -> ../.claude/skills
.dsh/skills    -> ../.claude/skills
.pencode/skills、.qoder/skills、.trae/skills 等同样指向 ../.claude/skills
```

- 允许的形式只有 `<工具目录>/skills -> ../.claude/skills`；工具目录按需创建，不强制存在。
- 论文资料目录（`01-raw` ~ `08-reading`）内**不得使用任何符号链接**。
- 不要用符号链接作为跨目录引用的"捷径"或旧名别名，例如 `03-notes/<论文标题>/xxx.md -> ../../...`、`01-raw/旧名.pdf -> 新名.pdf`。
- 不要用自引用符号链接伪造目录结构，例如 `images/<论文标题> -> .`。
- 跨目录引用（其他论文的笔记、图片、原文 PDF/Markdown）一律使用**相对路径**直接指向真实文件本身，不经过符号链接中转。
- 图片真实存放于 `03-notes/<YYYY-MM>/<论文标题>/images/`（引用文献图片放 `images/<被引用论文标题>/`），不使用链接替代。
- 移动或删除真实文件时，同步更新所有引用它的相对链接，确保不存在悬空引用。

## 01-raw：原始论文

`01-raw/` 仅保存用户提供或下载的原始 PDF，是论文资料的原始来源与入库时间的权威记录。

```text
01-raw/
├── README.md
├── index.md
└── YYYY-MM/
    ├── README.md
    └── <论文标题>.pdf
```

- 文件名使用论文标题主干，并与 `02-markdown/YYYY-MM/` 中对应 Markdown 文件保持相同主干。
- 不在此目录保存解析文本、笔记、图片副本或翻译结果。
- 新文件应保留 PDF 原始内容，不修改或覆盖用户已有 PDF；目标文件已存在时停止下载，不覆盖。
- `01-raw/YYYY-MM/README.md` 记录本月各论文的**入库日期**、标题与主干；`01-raw/index.md` 汇总全部论文并标注各阶段状态。

## 02-markdown：PDF 解析结果

`02-markdown/` 仅保存从 `01-raw/` 中论文 PDF 解析得到的 Markdown 文本。

```text
02-markdown/
├── README.md
├── index.md
└── YYYY-MM/
    ├── README.md
    └── <论文标题>.md
```

- 目录与文件名主干必须与对应 `01-raw/YYYY-MM/<论文标题>.pdf` 完全一致。
- 解析 Markdown 是论文内容来源，不是人工精读笔记；不要在其中混入个人批注或 AI 分析。
- 解析结果是机器产物（例如 MinerU 输出），其中的图片可能仍是解析服务的远程 URL；远程图片不算跨目录引用，如需本地化应另行处理。
- 尚未解析的论文在 `02-markdown/index.md` 与月份 README 中登记为 `未解析`，解析失败登记原因，不创建空文件。

## 03-notes：论文阅读笔记

每篇论文必须拥有自己的目录，且位于其入库月份下。AI 生成的精读固定命名为 `精读.md`；用户补充的阅读想法、问题、复现实验等也放在同一论文目录，使用内容语义明确的文件名。

```text
03-notes/
├── README.md
├── index.md
└── YYYY-MM/
    ├── README.md
    └── <论文标题>/
        ├── 精读.md
        ├── <用户笔记主题>.md
        └── images/
            ├── fig1.png
            ├── fig2.png
            └── <被引用论文标题>/
                ├── fig1.png
                └── fig2.png
```

- 月份为该论文的入库月份；`03-notes/index.md` 记录精读状态、开始与完成日期、优先级。
- `精读.md` 是 AI 精读的唯一固定入口（`paper-analyze` skill 默认写入该文件）。

### 图片管理决策

采用**每篇论文目录内管理图片**的方式，不使用全局的 `03-notes/images/` 图片池。

- 本篇论文的图片直接放在 `03-notes/YYYY-MM/<论文标题>/images/` 下，例如 `fig1.png`、`fig2-method.png`。
- 引用其他论文的图片，放在 `images/<被引用论文标题>/` 下，防止与本篇图片或其他引用文献的图片同名冲突。
- 不要为没有图片的本篇或引用文献创建空图片目录。
- 笔记使用标准相对路径：

```markdown
![本篇论文图](images/fig1.png)
![引用文献图](images/<被引用论文标题>/fig1.png)
```

- 图片必须保存为本地文件并使用标准 Markdown 图片语法；不要依赖远程图片 URL、绝对路径、特定笔记软件语法或符号链接。
- 将图片与论文笔记置于同一目录树，便于整篇笔记连同资产一起移动、备份和分享。

## 04-equation_problem：公式解读

目录名 `04-equation_problem/` 用于存放论文的公式解读。每篇论文在此目录下建立对应文件夹；该文件夹是这篇论文全部公式相关材料的唯一归属位置。**本目录不按月份分层**，只按论文标题建立目录。

```text
04-equation_problem/
├── README.md
└── <论文标题>/
    ├── <AI 公式伴读或全流程解读>
    ├── <用户的理解与推导>
    └── <扫描图片、附件或其他公式材料>
```

- 不按单条公式强制拆分文件，也不限制该论文文件夹内的文件名、文件格式或子目录结构。
- 该目录可以包含 AI 的公式全流程解读、AI 伴读解释、用户自己的理解与推导、扫描得到的图片或文档，以及其他与公式有关的材料。
- 只要求所有内容都位于对应的 `<论文标题>/` 下；不要把同一篇论文的公式材料散落在 `04-equation_problem/` 根目录或其他论文目录。
- 允许在论文尚未精读时先建立空的 `<论文标题>/` 目录作为占位。
- 公式解读可链接到对应的 `02-markdown/<YYYY-MM>/<论文标题>.md` 或 `03-notes/<YYYY-MM>/<论文标题>/精读.md`，无需复制整篇论文内容；跨目录引用一律使用文件相对路径。

## 05-books：学习参考书籍

`05-books/` 仅用于保存学习和研究所需的参考书籍，不按月份分层。书籍文件直接放在该目录下；如书籍数量较多，可按主题建立子目录。

```text
05-books/
├── README.md
└── <书名>.<扩展名>
```

## 06-translation：论文翻译结果

`06-translation/` 用于保存论文翻译结果。每篇论文在与入库月份一致的月份目录下建立一个与论文标题完全一致的文件夹，文件夹内可以同时保存不同翻译形式和文件格式的版本。

```text
06-translation/
├── README.md
├── index.md
└── YYYY-MM/
    ├── README.md
    └── <论文标题>/
        ├── 双栏对比.<扩展名>
        ├── 单独翻译.<扩展名>
        └── <其他翻译形式>.<扩展名>
```

- 论文文件夹名称必须与 `01-raw/YYYY-MM/`、`02-markdown/YYYY-MM/`、`03-notes/YYYY-MM/` 中的 `<论文标题>` 一致。
- 翻译文件只放在对应论文文件夹内，不放在 `06-translation/` 根目录。
- 双栏对比、单独翻译等形式可以使用 Markdown、DOCX、PDF 等合适格式；文件名保留形式名称，扩展名标明实际格式。
- 翻译结果属于独立资产，不覆盖 `01-raw` 中的原始 PDF 或 `02-markdown` 中的解析结果。
- 尚未翻译的论文**不预建空目录**，在 `06-translation/index.md` 与月份 README 中登记为 `未翻译`。

## 07-research：研究领域调研

`07-research/` 用于保存围绕研究领域、技术方向、方法体系或工具生态开展的专题调研工作，不按月份分层。该目录中的内容可以综合多篇论文、官方文档、实践资料和个人分析，不要求与某一篇论文建立一一对应关系。

```text
07-research/
├── README.md
└── YYYY-MM-DD-<调研主题>.<扩展名>
```

- 调研文件统一使用"年月日 + 调研主题"的命名方式，例如 `2026-09-15-大模型训练主流框架调研_2021-2026.md`；这是本工作区允许文件名带日期的场景之一。
- 日期使用 `YYYY-MM-DD` 格式，表示该调研主题的创建或主要更新日期；主题名称应简洁、明确，能够反映调研范围。
- 调研内容可使用 Markdown、表格、图片或其他适合的附件格式；如附件较多，可为单项调研建立同名目录进行组织。
- 若调研内容明确针对某篇论文，仍可通过相对路径链接到 `01-raw`、`02-markdown`、`03-notes` 或其他相关目录中的真实文件，不复制已有资料。
- `README.md` 用于说明本目录用途和命名规范，不作为具体调研报告。

## 08-daily：每日检索日报

`08-daily/` 用于保存每日论文检索与顶会推荐产物，按日期目录隔离。

```text
08-daily/
├── README.md
└── YYYY-MM-DD/
    ├── 今日检索.md
    ├── 顶会论文推荐.md
    ├── daily-editorial.json
    ├── search_result.json
    └── _index.json
```

- 日期目录名使用 `YYYY-MM-DD`，是日报的检索日期；日报文件名固定为 `今日检索.md`，顶会推荐为 `顶会论文推荐.md`。
- 日报中的本地链接必须是从日报文件出发的**文件相对路径**，例如 `../../01-raw/2026-09/<论文标题>.pdf`、`../../03-notes/2026-09/<论文标题>/精读.md`。
- 日报引用的论文必须先核验真实资产存在；不存在的资料用远程链接或标注"暂无"，不得捏造本地路径。
- `daily-editorial.json` 保存本次人工撰写的概览与前 3 篇分析，便于在链接或目录调整后重新渲染日报。

## 08-reading：交互式精读过程材料

`08-reading/` 保存公式伴读/交互式精读的**过程材料**（逐单元问答记录、全局推理中间草稿），不按月份分层，只按论文标题建立目录。

```text
08-reading/
├── README.md
└── <论文标题>/
    ├── <论文标题>_精读笔记.md
    └── <论文标题>_全局推理_过程.md
```

- 过程材料留在本目录；可独立阅读的定稿写入 `04-equation_problem/<论文标题>/`，两者不混放。
- 目录名主干与 `01-raw` 等其他目录保持一致。

## 新论文固定流程

以后新增论文一律按以下顺序执行：

1. 下载到当前月份的 `01-raw/YYYY-MM/`（用 `fetch_pdfs.py`，目标文件已存在即停止）。
2. 更新 `01-raw/YYYY-MM/README.md` 的入库清单（记录入库日期、标题、主干）。
3. 更新 `01-raw/index.md` 统一登记表。
4. 在 `02-markdown/YYYY-MM/README.md` 与 `index.md` 登记为 `未解析`。
5. 解析完成后更新 `02-markdown` 的解析状态与文件路径（`已解析` / `解析失败` + 原因）。
6. 精读开始时在 `03-notes/YYYY-MM/` 建立论文目录并登记 `精读中` + 开始日期；完成后登记 `已精读` + 完成日期。
7. 翻译开始时在 `06-translation/YYYY-MM/` 建立论文目录并登记 `翻译中`；完成后登记 `已翻译` + 翻译形式与完成日期。
8. 同步更新各阶段根索引与月份 README（推荐直接执行 `sync_indexes.py` 后人工补状态与日期）。

## 执行检查清单

创建或更新论文资料前，依次确认：

1. `01-raw/YYYY-MM/` 中存在对应 PDF，`02-markdown/YYYY-MM/` 使用相同文件名主干，且月份为该论文的入库月份。
2. `03-notes/YYYY-MM/<论文标题>/精读.md` 是 AI 精读的唯一固定入口。
3. 本篇及引用文献的图片均位于该论文目录的 `images/` 树中。
4. 所有该论文的公式推导、AI 伴读、用户材料和相关附件都位于 `04-equation_problem/<论文标题>/` 下。
5. 所有 Markdown 图片链接与跨目录引用均为可解析的相对路径（相对所在文件），且指向真实文件；不覆盖用户已有笔记。
6. 涉及的根目录与月份目录都有 `README.md`，四个阶段目录的 `index.md` 状态与磁盘一致；未解析/未精读/未翻译论文均已登记。
7. 研究调研文件遵循 `YYYY-MM-DD-<调研主题>` 命名规范，并放置在 `07-research/` 下。
8. 论文资料目录内不存在符号链接；技能入口 symlink 仅允许 `<工具目录>/skills -> ../.claude/skills`（如 `.agents/skills`、`.dsh/skills`）。
9. 项目内不存在 `task_plan.md`、`findings.md`、`progress.md` 等规划或过程文件。
10. 执行 `python .claude/skills/paper-daily/scripts/sync_indexes.py --check-links`，确认无悬空链接、无月份/主干错位。

## 单篇论文的资料目录对照示例

以入库月份 `2026-09` 的论文 `<论文标题>` 为例，各目录的对应关系：

```text
01-raw/
└── 2026-09/
    └── <论文标题>.pdf

02-markdown/
└── 2026-09/
    └── <论文标题>.md

03-notes/
└── 2026-09/
    └── <论文标题>/
        ├── 精读.md
        ├── <用户笔记主题>.md
        └── images/
            ├── fig1.png
            └── <被引用论文标题>/
                └── fig1.png

04-equation_problem/
└── <论文标题>/
    └── <公式解读、推导、附件等>

06-translation/
└── 2026-09/
    └── <论文标题>/
        └── <双栏对比、单独翻译等翻译文件>

08-reading/
└── <论文标题>/
    └── <论文标题>_精读笔记.md
```

用户在当前对话中给出的明确要求优先于本文件；发生冲突时，应先向用户确认再修改布局，并在修改后同步更新索引与所有引用。
